import 'package:flutter/material.dart';

class LocationSharePage extends StatelessWidget {
  const LocationSharePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('위치 공유'));
  }
}
