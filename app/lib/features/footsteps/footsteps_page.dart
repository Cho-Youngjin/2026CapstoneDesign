import 'package:flutter/material.dart';

class FootstepsPage extends StatelessWidget {
  const FootstepsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('발걸음'));
  }
}
