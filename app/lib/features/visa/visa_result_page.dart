import 'package:flutter/material.dart';

class VisaResultPage extends StatelessWidget {
  const VisaResultPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('비자 판정 결과'));
  }
}
